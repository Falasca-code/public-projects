# Roadmap futuro

Uma futura fonte adicional poderá implementar um adapter próprio para SNMP direto, atrás de uma capability flag como `FEATURE_ACTIVE_SNMP_DISCOVERY=false`. Isso não faz parte da V1 e não está implementado. O contrato canônico deve continuar isolando adapters da topologia e da UI.

